/* If base is a multiple of factor, returns true. Otherwise, returns false. */
function isMultipleOf(base,factor){
    if (base % factor === 0) {
        return true;
    } else {
        return false;
    }
}